# Copy to .env.test (gitignored). These tests run against the LIVE Supabase
# project, so every write is scoped to fixture accounts (*-test@levi-fixture.local).
TARGET_SUPABASE_URL=https://aaxvylonfejefadiotjv.supabase.co
TARGET_SUPABASE_ANON_KEY=
TARGET_SUPABASE_SERVICE_ROLE_KEY=
E2E_BASE_URL=http://localhost:3000

# Safety switch. No test or script may write to the live project unless this is
# exactly "1". Leave it empty to run read-only suites.
ALLOW_WRITES_TO_LIVE=
